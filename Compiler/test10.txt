const int MaxN=100, a = -200 , a3 = +100, a1 = 0,a2 = 23;
const char b = 'c',d = 'c';
int list[100],l,t;
char a5[20],e,a4[20],t1;
void recor_printU(int N)
{
    int i,s;
    l = b + d;
    s = 2*3-4*(3*1+(3+4+5+6*(7*8*9-23))*34-21)*14;
    t = -3*-4-2/1;
    t1 = -2/-1-+6/-2;
    for(i=0;i!=N;i=i+1){
        list[i] = i;
        t = list[i];
        printf(" ",list[i]);
    }
}
void recor_printD(int N)
{
    if(N!=0){
    printf(" ",N);
    recor_printD(N-1);
    }
}
int Isright(int N,char type)
{
    const char b = 'c',d = 'c';
    if(N>=100){
        printf("The number is too large!");
        return (0);
    }
    if(N>0){
        if(type=='u')
            return (1);
        if(type=='d')
            return (2);
    }
    return (0);
}
void sumandmul(int N)
{
    int i,sum,mul;
    sum=0;
    mul= 1;
    for(i=0;i!=N;i=i+1){
        sum = sum + i;
    }
    printf(sum);
    printf(" ");
    for(i=0;i!=N;i= i+1){
        if(list[i]!=0)
            mul = mul * list[i];
    }
    printf(mul);
}

void main()
{
    int N,t;
    char type;
    printf("Please enter the number and arrangement of ways, u said ascending order, d said descending order!");
    scanf(N,type);
    t = Isright(N,type);

    switch(t){
        case 1:
            recor_printU(N);
        case 2:{
            l=0;
            recor_printD(N);

            }
        case 0:
            printf("Input does not match the syntax, exit!");
        default:
            printf("Input does not match the syntax, exit!");
    }
    if(t!=0){
        printf("Whether or not to sum and mul these number, 0 or 1:");
        scanf(t);
        if(t==1)
            sumandmul(N);
        else
            printf("The program is over!");
    }
    printf("Thanks for trying, Bye!");
}