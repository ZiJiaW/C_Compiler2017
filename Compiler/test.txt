const int a = 10, b = 5;
const char hk = '1', lo = 'g';
int cot;
char ch, po;
int lex, ko, qi;
int sum(int a, int b)
{
    const int foot = 100;
    int s_oi;
    s_oi = foot*12/3+9*(1+hk-'0');
    return (a+b*s_oi);
}
void foo()
{
    lex = 102;
    ko = sum(lex, po);
}
char link(char a, int b)
{
    return (a+b);
}
int get_10()
{
    return (10);
}
void main()
{
    const int d = 5;
    int s, pssum;
    char p;
    int re;
    s=6;
    p='a';
    pssum = sum(p,s);
    re = sum(pssum,d);
    printf("hello:",re);
    printf(link(p, d));
    printf(get_10());
    return;
}